function funcao(a, b, c) {
/*     let total = 0;
    for(arumento of arguments) {
        total += arumento;
    }
    console.log(total, a, b, c) */
    console.log(a,b,c)
}

funcao(1,2,3);

function contas (operador, acumulador, ...numeros) {
    for (let numero of numeros) {
        if (acumuladora += numero);
        
    }
    console.log(acumuladora);
}

contas('+', 0, 1, 2);