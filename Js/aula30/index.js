const frutas = ['maca', 'pera', 'uva', 'saladamista'];

for (let i in frutas) {
}

const pessoa = {
    nome: 'Luix',
    sobrenome: 'Otavio',
    idade: 30
};

for (let chave in pessoa){
    console.log(chave, pessoa[chave])
}